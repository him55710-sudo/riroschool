// This directory holds versions of Prompts for the AI models.
// Currently hardcoded in worker stages, but can be migrated here for v2.

export const PromptsV1 = {
    OUTLINE: `... (see apps/worker/src/stages/plan.ts)`,
    WRITE: `... (see apps/worker/src/stages/write.ts)`
};
